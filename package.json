{
  "scripts":{
    "start": "cat jsbattle.config.json | PORT=${PORT:-8080} envsubst > jsbattle.config.resolved.json && npx jsbattle start --config jsbattle.config.resolved.json"
  },
  "dependencies": {
    "jsbattle": "^3.7.1"
  }
}
